// Stub layout for the protected (app) route group. The full app shell
// (sidebar, topbar, theme toggle) is built in Phase 4 (T-4.x).
export default function AppLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
